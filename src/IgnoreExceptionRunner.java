import IgnoreException.IgnoreExceptionInterface;
import IgnoreException.IgnoreExceptionProxy;

/**
 * This is the type IgnoreExceptionRunner.
 *
 * @author <a href="mailto:jsvg88@scania.com">jsvg88</a>
 * @since 2018-02-21
 */
public class IgnoreExceptionRunner {
/*    IgnoreExceptionInterface impl = new java.lang.reflect.Proxy().newProxyInstance(Pool.Impl.class.getClassLoader(),
            Pool.Impl.class.getInterfaces(), IgnoreExceptionProxy(new Pool.Impl<>()));
            */
}
